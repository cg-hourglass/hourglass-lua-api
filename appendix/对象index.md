# 对象index

对象index是本引擎脚本里代表一个游戏对象的整数句柄，可以是：

- 普通玩家对象
- 宠物对象
- NPC对象
- 战斗中的敌人对象

对象index底层是角色管理器中分配的一个稳定索引。脚本通过它调用 `Char.*`、`NLG.*` 等接口，按索引解析出对应的对象，读取或修改该对象在服务端内存中保存的数据，也可以结合其它开放的接口对该对象进行操作。对象失效后（角色下线、宠物/敌人被移除等）再使用该 index，会被引擎当作“对象不可用”处理，具体返回值以各函数页的说明为准。
