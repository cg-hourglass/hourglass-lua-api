<!-- Generated. DO NOT EDIT. -->
# SetStatus

## Pet.SetStatus(CharIndex, PetSlot, Status)

### 函数功能

设置玩家指定宠物栏位上宠物的出战状态。

### 参数说明

- CharIndex: [数值型](../appendix/数值型.md) 目标玩家的对象index。
- PetSlot: [数值型](../appendix/数值型.md) 宠物栏位置，取值 0-4。
- Status: [数值型](../appendix/数值型.md) 宠物状态，取值见备注中的宠物状态常量。

### 返回值

1 成功，0 失败。

## 参考实例

```lua
Pet.SetStatus(_player, 0, %宠物状态_战斗%); -- 让 0 号栏位的宠物出战
Pet.SetStatus(_player, 1, %宠物状态_待命%);
```

### 备注

宠物状态常量：%宠物状态_无%（0）、%宠物状态_待命%（1）、%宠物状态_战斗%（2）、%宠物状态_休息%（3）。

本版本实际只接受前三个：传入 %宠物状态_休息%（3）会被状态普查拒绝，
整次调用返回 0。空槽位只能设为 %宠物状态_无%。

状态值还可以按位或上 16（散步宠物标志，常量表中没有对应常量）。散步标志同时
只能有一只宠物持有，并且该宠物必须拥有散步技能（tech id 25600），否则整次调用失败。

其余失败条件：PetSlot 不在 0-4 范围内；CharIndex 不是玩家；玩家正处于战斗中；
宠物栏中存在主人指针不匹配的宠物（此时会先清理这些槽位再失败）；
出战宠物超过 1 只；待命宠物超过 3 只；已有出战宠物时待命宠物超过 2 只；
任一被写入的槽位上宠物已死亡。

**部分生效风险**：设置阶段是逐槽写入的，中途遇到空槽或已死亡的宠物会立即返回 0，
但此前若干槽位的写入已经生效且不会回滚，这是本版本的既有行为。
